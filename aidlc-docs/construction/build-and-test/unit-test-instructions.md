# Unit Test Instructions

## Prerequisites
- Jest framework installed
- Node Options configured for ESM

## Instructions
Execute all BDD feature steps and unit tests by running:
`npm test`

To run specific test modules (like the workflow loader):
`npm run test:workflow-loader`
