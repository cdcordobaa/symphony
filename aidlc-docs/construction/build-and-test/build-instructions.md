# Build Instructions

## Prerequisites
- Node.js 18+
- TypeScript compiler (`tsc`)

## Instructions
1. Install dependencies:
   `npm install`
2. Build is handled automatically in development using `tsx`. If a production build is needed, you can add `"build": "tsc"` to `package.json` and run `npm run build`.
